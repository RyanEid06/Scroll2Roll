# Cloudflare Website

`website/` is the static Scroll2Roll product and download website. It is not a browser port of the casino.

The prepared site will use HTML, CSS, and only minimal JavaScript, share the application color tokens, and include product identity, Blackjack capabilities proven by tests, screenshots or honest placeholders, Windows requirements, installation, version, controls, troubleshooting, privacy, play-money-only language, and a release download action.

No paid Cloudflare product may be added without owner approval. Current free-tier static-asset and file-size limits must be verified from authoritative Cloudflare documentation before a release package location is chosen. If the package is too large, documentation will present safe alternatives instead of silently changing providers.

Deployment is intentionally not performed until the owner explicitly approves publishing.

